CREATE TABLE LOGS (
    LogID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    TableName VARCHAR2(50) NOT NULL,
    Action VARCHAR2(20) NOT NULL, 
    RecordID NUMBER, 
    UserName VARCHAR2(50),
    LogDate TIMESTAMP DEFAULT SYSTIMESTAMP,
    Details CLOB 
);
